<!DOCTYPE html>
<html>
  <head>
    <title>Bookstore</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Books currently available</h1>
    <% for (let i = 0; i < result.length; i++) { %>
    <p><%= result[i].title + ': ' + result[i].year %></p>
      <% } %>
  </body>
</html>
